# More Operations on Sets

$S=\{ n^2\mid n \in \mathbb{N} \}$
We could also define this set by writing $S=\{ x \mid \exists n \in \mathbb{N}(x=n^2) \}$

Therefore $x \in \{ n^2 \mid n \in \mathbb{N} \}$ means the same thing as $\exists n\in \mathbb{N}(x=n^2)$.

Similar notation is often used if the elements of a set have been numbered. For example, suppose we wanted to form the set whose elements are the first 100 prime numbers, calling them $p_{1},p_{2},p_{3},\dots$ In other words, $p_{1}=2$, $p_{2}=3$, $p_{3}=5$, and so on. Then the set we are looking for would be the set $P=\{ p_{1},p_{2},p_{3},\dots p_{100} \}$. Another way of describing this set would be to say that it consists of all numbers $p_{i}$, for $i$ an element of the set $I=\{ 1,2,3,\dots,100 \}=\{ i\in \mathbb{N} \mid 1\leq i\leq 100 \}$. This could be written $P=\{ p_{i}\mid i \in I \}$. Each element $p_{i}$ in this set is identified by a number $i \in I$, called the **index** of the element. A set defined in this way is sometimes called an **indexed family**, and $I$ is called the **index set**.

Although the indices for an indexed family are **often numbers**, they need not be. For example, suppose $S$ is the set of all students at your school. If we wanted to form the set of all mothers of students, we might let $m_{s}$ stand for the mother of $s$, for any student $s$. This is an indexed family in which the index set is $S$, the set of all students. Each mother in the set is identified by naming the student who is her child. Note that we could also define this set using an elementhood test, by writing $M=\{ m \mid m \text{ is the mother of some student} \}=\{ m \mid \exists s \in S(m=m_{s}) \}$. In general, any indexed famliy $A=\{ x_{i}\mid i \in I \}$ can also be defined as $A=\{ x \mid \exists i \in I(x=x_{i}) \}$. It follows that the statement $x \in \{ x_{i} \mid i \in I \}$ means the same thing as $\exists i \in I(x=x_{i})$.

Example 2.3.1. Analyze the logical forms of the following statements by writing out the definitions of the set theory notation used.

1.$y \in \{ \sqrt[3]x{  }\mid x \in \mathbb{Q} \}$.
2.$\{ x_{i}\mid i \in I \}\subseteq A$.
3.$\{ n^2 \mid n \in \mathbb{N} \}$ and $\{ n^3 \mid n \in \mathbb{N} \}$ are not disjoint.

1.$\exists x \in \mathbb{Q}(y=\sqrt[3]{ x })$.
2.By the definition of subset we must say that every element of $\{ x_{i} \mid i \in I \}$ is also an element of $A$, so we could start by writing $\forall x(x \in \{ x_{i}\mid i \in I \}\to x \in A)$. Filling in the meaning of $x \in \{ x_{i}\mid i \in I \}$, which we worked out earlier, we would end up with $\forall x(\exists i \in I(x=x_{i})\to x \in A)$. But since the elements of $\{x_{i}\mid i \in I\}$ are just the $x_{i}$'s, for all $i \in I$, perhaps an easier way of saying that every element of $\{ x_{i} \mid i \in I \}$ is an element of $A$ would by $\forall i \in I(x_{i}\in A)$. The two answers we have given are equivalent, but showing this would require the methods we will be studying in Chapter 3.
3.We must say that the two sets have a common element, so one solution is start by writing $\exists x(x \in \{ n^2\mid n\in \mathbb{N} \}\land x \in \{ n^3 \mid n \in \mathbb{N} \})$. However, as in the last statement, there is an easier way. An element common to the two sets would have to be the square of some natual number and also the cube of some(possibly different) natural number. Thus, we could say that there is such a common element by saying $\exists n \in \mathbb{N}\exists m \in \mathbb{N}(n^2=m^3)$. Note that it would be wrong to write $\exists n \in \mathbb{N}(n^2=n^3)$, because this wouldn't allow for the possibility of the two natural numbers being different. By the way, this statement is true, since $64=8^2=4^3$, so $64$ is an element of both sets.

$\mathcal{F}=\{ \{ 1,2,3 \},\{ 4 \},\emptyset \}$. Sets such as $\mathcal{F}$, whose elements are all sets, are sometimes called **famielies** of sets.

Definition 2.3.2. Suppose $A$ is a set. The **power set** of $A$, denoted $\mathscr{P}(A)=\{ x\mid x \subseteq A \}$.

$A=\{ 7,12 \}$, $\mathscr{P}(A)=\{ \emptyset,\{ 7 \},\{ 12 \},\{ 7,12 \} \}$. 

Although $\emptyset$ has no elements, it does have one subset. $\mathscr{P}(\emptyset)=\{ \emptyset \}$.

Any time you are working with some subsets of a set $X$, it may be helpful to remember that all of these subsets of $X$ are elements of $\mathscr{P}(X)$, by the definition of power set. For example, if we let $C$ be the set of all courses offered at your school, then each of the sets $C_{s}$ discussed earlier is a subset of $C$. Thus, for each student $s$, $C_{s}\in \mathscr{P}(C)$. This means that every element of the family $\mathcal{F}=\{ C_{s}\mid s \in S \}$ is an element of $\mathscr{P}(C)$, so $\mathcal{F}\subseteq \mathscr{P}(C)$.

Example 2.3.3. Analyze the logical forms of the following statements.
1.$x \in \mathscr{P}(A)$
2.$\mathscr{P}(A)\subseteq \mathscr{P}(B)$
3.$B\in \{ \mathscr{P}(A)\mid A\in \mathcal{F} \}$.
4.$x \in \mathscr{P}(A\cap B)$.
5.$x \in \mathscr{P}(A)\cap \mathscr{P}(B)$.

1.By the definition of power set, the elements of $\mathscr{P}(A)$ are the subsets of $A$. Thus, to say $x \in \mathscr{P}(A)$ means $x\subseteq A$, which we already know can be written as $\forall y(y\in x\to y\in A)$.

2.By the definition of subset, this means $\forall x(x \in \mathscr{P}(A)\to x \in \mathscr{P}(B))$. Now, writing out $x \in \mathscr{P}(A)$ and $x \in \mathscr{P}(B)$ as before, we get $\forall x[\forall y(y\in x\to y\in A)\to \forall y(y\in x\to y\in B)]$.

3.$\exists A\in F(B=\mathscr{P}(A))$. Now, to say that $B=\mathscr{P}(A)$ means that the elements of $B$ are precisely the subsets of $A$, or in other words $\forall x(x \in B\leftrightarrow x\subseteq A)$. Filling this in, and writing out the definition of subset, we get our final answer, $\exists A\in F\forall x(x \in B\leftrightarrow \forall y(y\in x\to y\in A))$.

4.
$\forall y(y\in x\to y\in A\cap B)$. $\forall y(y\in x\to (y\in A\land y\in B))$.

5.
$(x \in \mathscr{P}(A))\land(x \in \mathscr{P}(B))$. $\forall y(y\in x\to y\in A)\land \forall y(y\in x\to y\in B)$.


$\mathcal{F}= \{ C_{s} \mid s \in S \}$, where $S$ is the set of all students and for each student $s$, $C_{s}$ is the set of all courses that $s$ has taken. If we wanted to know which courses had taken by all students, we would need to find those elements that all the sets in $\mathcal{F}$ have in common. The set of all these common elements is called the intersection of the family $\mathcal{F}$ and is written $\bigcap \mathcal{F}$. Similarly, the union of the family $\mathcal{F}$, written $\bigcup \mathcal{F}$, is the set resulting from throwing all the elements of all the sets in $\mathcal{F}$ together into one set. In this case, $\bigcup \mathcal{F}$ would be the set of all courses that had been taken by any student.

Example 2.3.4. Let $\mathcal{F}=\{ \{ 1,2,3,4 \},\{ 2,3,4,5 \},\{ 3,4,5,6 \} \}$. Find $\bigcap \mathcal{F}$ and $\bigcup \mathcal{F}$.
$\bigcap \mathcal{F}=\{ 1,2,3,4 \}\cap \{ 2,3,4,5 \}\cap \{ 3,4,5,6 \}=\{ 3,4 \}$
$\bigcup \mathcal{F}=\{ 1,2,3,4 \}\cup \{ 2,3,4,5 \}\cup \{ 3,4,5,6 \}=\{ 1,2,3,4,5,6 \}$

Definition 2.3.5. Suppose $\mathcal{F}$ is a family of sets. Then the intersection and union of $\mathcal{F}$ are the sets $\bigcap \mathcal{F}$ and $\bigcup \mathcal{F}$ defined as follows:

$$
\begin{align}
\bigcap \mathcal{F}&=\{ x\mid\forall A\in \mathcal{F}(x \in A) \}=\{ x\mid\forall A(A\in \mathcal{F}\to x \in A) \} \\
\bigcup \mathcal{F}&=\{ x\mid \exists A\in \mathcal{F}(x \in A) \}= \{ x\mid \exists A(A\in \mathcal{F}\land x \in A) \}
\end{align}

$$

Some mathematicians consider $\bigcap \mathcal{F}$  to be **undefined** if $\mathcal{F}=\emptyset$. We will use the notation $\bigcap \mathcal{F}$ only when $\mathcal{F}\neq \emptyset$.

If $A$ and $B$ are any two sets and $\mathcal{F}=\{ A, B \}$, then $\bigcap \mathcal{F}=A\cap B$, $\bigcup \mathcal{F}=A\cup B$.

Example 2.3.6. Analyse the logical forms of the following statements.
1.$x \in \mathcal{F}$.
2.$\bigcap \mathcal{F}\not\subseteq\bigcup \mathcal{G}$.
3.$x \in \mathscr{P}\left( \bigcup \mathcal{F} \right)$.
4.$x \in \bigcup \{ \mathscr{P}(A)\mid A\in \mathcal{F} \}$.

1.By the definition of the intersection of a family of sets, this means $\forall A\in \mathcal{F}(x \in A)$. or equivalently, $\forall A(A\in \mathcal{F}\to x \in A)$.
2.$\exists x\left( x \in \bigcap \mathcal{F}\land x\not\in \bigcup \mathcal{G} \right)$, By the definition of the union of a family of sets, $x \in \bigcup \mathcal{G}$ means $\exists A\in \mathcal{G}(x \in A)$, so $x\not\in \bigcup \mathcal{G}$ means $\neg \exists A\in \mathcal{G}(x \in A)$. By the quantifier negation laws, this is equivalent to $\forall A\in \mathcal{G}(x\not\in A)$.  Putting this all together, our answer is $\exists x[\forall A\in F(x \in A)\land \forall A\in \mathcal{F}(x\not\in A)]$.

3.Because the union symbol occurs within the power set notation, we start by writing out the definition of power set. As in Example 2.3.4, we get $x\subseteq \bigcup \mathcal{F}$, or in other words $\forall y\left( y\in x\to y\in \bigcup \mathcal{F} \right)$. Now we use the definition of union to write out $y\in \bigcup \mathcal{F}$ as $\exists A\in \mathcal{F}(y\in A)$. The final answer is $\forall y(y\in x\to \exists A\in \mathcal{F}(y\in A))$.

4.We start by writing out the definition of union. According to this definition, the statement means that $x$ is an element of at least one of the sets $\mathscr{P}(A)$, for $A\in \mathcal{F}$. In other words, $\exists A\in \mathcal{F}(x \in \mathscr{P}(A))$.  Inserting our analysis of the statement $x \in \mathscr{P}(A)$ from example 2.3.3, we get $\exists A\in \mathcal{F}\forall y(y\in x\to y\in A)$.

An alternative notation is sometimes used for the union or intersection of indexed family of sets. Suppose $\mathcal{F}=\{ A_{i}\mid i \in I \}$, where each $A_{i}$ is a set. Then $\bigcap \mathcal{F}$ would be the set of all elements common to all the $A_{i}$'s, for $i \in I$, and this can also be written as $\bigcap_{i\in I}A_{i}$. In other words,
$$
\bigcap \mathcal{F}= \bigcap_{i \in I}A_{i}=\{ x \mid \forall i \in I(x \in A_{i}) \}
$$
Similarly, an alternative notation for $\bigcup\mathcal{F}$ is $U_{i \in I}A_{i}$, so 
$$
\bigcup \mathcal{F}= \bigcup_{i \in I}A_{i} = \{ x \mid \exists i \in I(x \in A_{i}) \}
$$

Example 2.3.7. Let $I=\{ 1,2,3 \}$, and for each $i \in I$ let $A_{i}= \{ i, i+1, i+2, i+3 \}$. Find $\bigcap_{i\in I}A_{i}$ and $\bigcup_{i \in I}A_{i}$.

$$
A_{1}=\{ 1,2,3,4 \}, A_{2}=\{ 2,3,4,5 \},A_{3}=\{ 3,4,5,6 \}
$$
Then
$$
\bigcap_{i \in I}A_{i}= A_{1}\cap A_{2}\cap A_{3}=\{ 1,2,3,4 \}\cap \{ 2,3,4,5 \}\cap \{ 3,4,5,6 \}=\{ 3,4 \}
$$
and similarly
$$
\bigcup_{i \in I}A_{i}=\{ 1,2,3,4 \}\cup \{ 2,3,4,5 \}\cup \{ 3,4,5,6 \}=\{ 1,2,3,4,5,6 \}
$$
Example 2.3.8. For this example our universe of discourse will be the set $S$ of all students. Let $L(x,y)$ stand for "$x$ likes $y$" and $A(x,y)$ for "$x$ admires $y$." For each student $s$, let $L_{s}$ be the set of all students that $s$ likes. In other words $L_{s}=\{ t\in S \mid L(s,t) \}$. Similarly, let $A_{s}=\{ t\in S\mid A(s,t) \}=\text{the set of all students that } s \text{ admires}$. Describe the following sets.

1.$\bigcap_{s \in S}L_{s}$.
2.$\bigcup_{s \in S}L_{s}$.
3.$\bigcup_{s \in S}L_{s}\setminus \bigcup_{s \in S}A_{s}$.
4.$\bigcup_{s \in S}(L_{s}\setminus A_{s})$.
5.$\left( \bigcap_{s \in S}L_{s} \right)\cap\left( \bigcap_{s \in S}A_{s} \right)$.
6.$\bigcap_{s \in S}(L_{s}\cap A_{s})$.
7.$\bigcup_{b \in B}L_{b}$, where $B=\bigcap_{s \in S}A_{s}$.

First of all, note that in general, $t \in L_{s}$ means the same thing as $L(s,t)$, and similarly $t \in A_{s}$ means $A(s,t)$.

1.$\bigcap_{s \in S}L_{s}=\{ t \mid\forall s \in S(t \in L_{s}) \}=\{ t\in S\mid\forall s \in S\,L(s,t) \}=$ the set of all students who are liked by all students.
2.
$\bigcup_{s \in S}L_{s}=\{ t\mid\exists s \in S(t \in L_{s}) \}=\{ t \in S\mid\exists s \in S\, L(s,t) \}=$ the set of all students who are liked by at least one student.
3.
$\bigcup_{s \in S}L_{s}\setminus \bigcup_{s \in S}A_{s}=\left\{  t \mid t \in \bigcup_{s \in S}L_{s}\land t\not\in \bigcup_{s \in S}A_{s}  \right\}=$ the set of all students who are liked by at least one student, but are not admired by any students.
4.
$\bigcup_{s \in S}(L_{s}\setminus A_{s})=\{ t\mid\exists s \in S(t\in L_{s}\setminus A_{s}) \}=\{ t \in S\mid\exists s \in S(L(s,t)\land \neg A(s,t)) \}=$ the set of all students $t$ such that some student likes $t$, but doesn't admire $t$.
5.
$\left( \bigcap_{s \in S}L_{s} \right)\cap\left( \bigcap_{s \in S}A_{s} \right)=\left\{  t\mid t \in \bigcap_{s \in S}L_{s}\land t \in \bigcap_{s \in S}A_{s}  \right\}=\{ t \mid\forall s \in S(t \in L_{s})\land \forall s \in S(t \in A_{s}) \}=$ $\{ t \in S\mid\forall s \in S\, L(s,t) \land \forall s \in S\,A(s,t) \}=$ the set of all students who are liked by all students and also admired by all students.

6.
$\bigcap_{s \in S}(L_{s}\cap A_{s})=\{ t \mid\forall s \in S(t \in L_{s}\cap A_{s}) \}=\{ t \in S\mid\forall s \in S(L(s,t)\land A(s,t)) \}=$ the set of all students who are both liked and admired by all students.

7.
$\bigcup_{b \in B}L_{b}=\{ t\mid\exists b \in B(t \in L_{b}) \}=\{ t \in S \mid \exists b(b \in B \land L(b,t)) \}$. But $B$ was defined to be the set of all students who are admired by students, so $b \in B$ means $b \in S\land \forall s \in S\,A(s,b)$. Inserting this, we get $\bigcup_{b\in B}L_{b}=\{ t \in S \mid\exists b(b \in S\land \forall s \in S\, A(s,b)\land L(b,t)) \}=$ the set of all students who are liked by some students who is admired by all students.

# Exercise
1.Analyze the forms of the following statements. You may use the symbols $\in,\not\in,=,\neq,\land,\lor,\to,\leftrightarrow,\forall,\exists$ in your answers, but not $\not\subseteq,\mathscr{P},\cap,\cup,\setminus,\{,\},\neg$. 
(a) $\mathcal{F}\subseteq \mathscr{P}(A)$.
(b) $A\subseteq \{ 2n+1\mid n \in \mathbb{N} \}$.
(c) $\{ n^2+n+1 \mid n \in \mathbb{N} \}\subseteq \{ 2n+1 \mid n \in \mathbb{N} \}$.
(d) $\mathscr{P}\left( \bigcup_{i \in I}A_{i} \right)\not\subseteq \bigcup_{i \in I}\mathscr{P}(A_{i})$.

(a) 
$\forall x(x \in \mathcal{F}\to x \in \mathscr{P}(A))$, which is equivalent to $\forall x(x \in \mathcal{F}\to \forall y(y \in x\to y \in A))$.
(b)
$\forall x(x \in A\to \exists n\in \mathbb{N}(x=2n+1))$.

(c)
$\forall x(\exists n \in \mathbb{N}(x=n^2+n+1)\to \exists n\in \mathbb{N}(x=2n+1))$.
simpler:
$\forall n\in \mathbb{N}\exists m\in \mathbb{N}(n^2+n+1=2m+1)$.

(d)
$\exists x\left( x \in \mathscr{P}\left( \bigcup_{i \in I}A_{i} \right) \land x\not\in \bigcup_{i \in I}\mathscr{P}(A_{i})\right)$ is equivalent to 
$\exists x\left( x \subseteq \bigcup_{i \in I}A_{i}\land \neg\left( x \in \bigcup_{i \in I}\mathscr{P}(A_{i}) \right) \right)$, which is equivalent to 
$\exists x\left( \forall y\left( y\in x\to y\in \bigcup_{i \in I}A_{i} \right)\land \neg(\exists i \in I(x \in \mathscr{P}(A_{i}))) \right)$, which is equivalent to 
$\exists x(\forall y(y\in x\to \exists i\in I(y\in A_{i}))\land \neg(\exists i\in I(\forall y(y\in x\to y\in A_{i}))))$, which is equivalent to
$\exists x(\forall y(y\in x\to \exists i\in I(y\in A_{i}))\land \forall i\in I\exists y(y\in x\land y\not\in A_{i}))$.

2.Analyze the forms of the following statements. You may use the symbols $\in,\not\in,=,\neq,\land,\lor,\to,\leftrightarrow,\forall,\exists$ in your answers, but not $\not\subseteq,\mathscr{P},\cap,\cup,\setminus,\{,\},\neg$. 

(a) $x \in \bigcup \mathcal{F}\setminus \bigcup \mathcal{G}$.
(b) $\{ x \in B \mid x \not\in C \}\in \mathscr{P}(A)$.
(c) $x \in \bigcap_{i \in I}(A_{i}\cup B_{i})$.
(d) $x \in\left( \bigcap_{i \in I}A_{i} \right)\cup\left( \bigcap_{i \in I}B_{i} \right)$.

(a) 
$x \in \bigcup \mathcal{F}\land x\not\in \bigcup \mathcal{G}$, which is equivalent to 
$\exists A\in \mathcal{F}(x \in A)\land \neg(\exists A\in \mathcal{G}(x \in A))$, which is equivalent to 
$\exists A\in \mathcal{F}(x \in A)\land \forall A\in \mathcal{G}(x\not\in A)$.
(b)
$\{ x \in B\mid x\not\in C \}\in \mathscr{P}(A)$, which is equivalent to
$\forall x(x \in \{ x \in B\mid x \not\in C \}\to x \in A)$, which is equivalent to 
$\forall x(x \in B\land x\not\in C\to x \in A)$

(c)
$x \in \bigcap_{i \in I}(A_{i}\cup B_{i})$ is equivalent to $\forall i\in I(x \in A_{i}\cup B_{i})$, which is equivalent to
$\forall i\in I(x \in A_{i}\lor x \in B_{i})$.

(d)
$x \in\left( \bigcap_{i \in I}A_{i} \right)\cup\left( \bigcap_{i \in I}B_{i} \right)$ is equivalent to $x \in \bigcap_{i \in I}A_{i}\lor x \in \bigcap_{i \in I}B_{i}$, which is equivalent to 
$\forall i\in I(x \in A_{i})\lor \forall i\in I(x \in B_{i})$.

3.We've seen that $\mathscr{P}(\emptyset)=\{ \emptyset \}$, and $\{ \emptyset \}\neq \emptyset$. What is $\mathscr{P}(\{ \emptyset \})$?
By the definition of power of set, $\mathscr{P}(A)=\{ x\mid x \subseteq A \}$, $\mathscr{P}(\{ \emptyset \})=\{ x\mid x \subseteq \{ \emptyset \} \}$, $\emptyset \subseteq \{ \emptyset \}$, $\{ \emptyset \}\subseteq \{ \emptyset \}$, $\mathscr{P}(\{ \emptyset \})=\{ \emptyset, \{ \emptyset \} \}$. 

4.Suppose $\mathcal{F}=\{ \{ \text{red, green, blue} \},\{ \text{orange, red, blue} \},\{ \text{purple, red, green, blue} \} \}$. Find $\bigcap \mathcal{F}$ and $\bigcup \mathcal{F}$.

$\bigcap \mathcal{F}=\{ \text{red, blue} \}$, $\bigcup \mathcal{F}=\{ \text{red, green, blue, orange, purple} \}$.

5.Suppose $\mathcal{F}=\{ \{ 3,7,12 \},\{ 5,7,16 \},\{ 5,12,23 \} \}$. Find $\bigcap \mathcal{F}$ and $\bigcup \mathcal{F}$.

$\bigcap \mathcal{F}=\emptyset$, $\bigcup \mathcal{F}=\{ 3,5,7,12,16,23 \}$.

6.Let $I=\{ 2,3,4,5 \}$, and for each $i\in I$ let $A_{i}=\{ i,i+1,i-1,2i \}$.
(a) List the elements of all the sets $A_{i}$, for $i\in I$.
(b) Find $\bigcap_{i \in I}A_{i}$ and $\bigcup_{i \in I}A_{i}$.

(a) 
$A_{2}=\{ 2,3,1,4 \}$, $A_{3}=\{ 3,4,2,6 \}$, $A_{4}=\{ 4,5,3,8 \}$, $A_{5}=\{ 5,6,4,10 \}$.
(b)
$\bigcap_{i \in I}A_{i}=\{ 4 \}$, $\bigcup_{i \in I}A_{i}=\{ 1,2,3,4,5,6,8,10 \}$.


7.Let  
  
$P=\{\text{Johann Sebastian Bach},\ \text{Napoleon Bonaparte},\ \text{Johann Wolfgang von Goethe},\ \text{David Hume},\ \text{Wolfgang Amadeus Mozart},\ \text{Isaac Newton},\ \text{George Washington}\}$
and let $Y=\{1750,1751,1752,\dots,1759\}$. For each $y\in Y$, let $A_y=\{p\in P\mid \text{the person }p\text{ was alive at some time during the year }y\}$. Find $\bigcup_{y\in Y}A_y$ and $\bigcap_{y\in Y}A_y$.

pass

8.Let $I=\{ 2,3 \}$, and for each $i\in I$ let $A_{i}=\{ i,2i \}$ and $B_{i}=\{ i,i+1 \}$.
(a) List the elements of the sets $A_{i}$ and $B_{i}$ for $i\in I$.
(b) Find $\bigcap_{i \in I}(A_{i}\cup B_{i})$ and $\left( \bigcap_{i \in I}A_{i} \right)\cup\left( \bigcap_{i \in I}B_{i} \right)$. Are they the same?
(c) In parts (c) and (d) of exercise 2 you analyzed the statements $x \in \bigcap_{i \in I}(A_{i}\cup B_{i})$ and $x \in\left( \bigcap_{i \in I}A_{i} \right)\cup\left( \bigcap_{i \in I}B_{i} \right)$. What can you conclude from your answer to part(b) about whether or not these statements are equivalent?

(a) $A_{2}=\{2,4  \}$, $A_{3}=\{ 3,6 \}$, $B_{2}=\{ 2,3 \}$, $B_{3}=\{ 3,4 \}$.
(b) $A_{2}\cup B_{2}=\{ 2,3,4 \}$, $A_{3}\cup B_{3}=\{ 3,4,6 \}$. $\bigcap_{i \in I}(A_{i}\cup B_{i})=\{ 3,4 \}$. $\bigcap_{i \in I}A_{i}=\emptyset$, $\bigcap_{i \in I}B_{i}=\{ 3 \}$, $\left( \bigcap_{i \in I}A_{i} \right)\cup\left( \bigcap_{i \in I}B_{i} \right)={3}$.
(c) They are not equivalent.

9.
(a) Analyze the logical forms of the statements $x \in \bigcup_{i \in I}(A_{i}\setminus B_{i})$, $x \in\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcup_{i \in I}B_{i} \right)$, and $x \in\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcap_{i \in I}B_{i} \right)$. Do you think that any of these statements are equivalent to each other?
(b) Let $I$, $A_{i}$ and $B_{i}$ be defined as in exercise $8$. Find $\bigcup_{i \in I}(A_{i}\setminus B_{i})$, $\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcup_{i \in I}B_{i} \right)$, and $\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcap_{i \in I}B_{i} \right)$. Now do you think any of the statements in part(a) are equivalent?

(a)
$x \in \bigcup_{i \in I}(A_{i}\setminus B_{i})$ is equivalent to $\exists i \in I(x \in A_{i}\setminus B_{i})$, which is equivalent to 
$\exists i \in I(x \in A_{i}\land x\not\in B_{i})$.

$x \in\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcup_{i \in I}B_{i} \right)$ is equivalent to $x \in\left( \bigcup_{i \in I}A_{i} \right)\land x\not\in \left( \bigcup_{i \in I}B_{i} \right)$, which is equivalent to 
$\exists i \in I(x \in A_{i})\land \neg\left( x \in \bigcup_{i \in I}B_{i} \right)$, which is equivalent to 
$\exists i\in I(x \in A_{i})\land \neg \exists i\in I(x \in B_{i})$, which is equivalent to 
$\exists i\in I(x \in A_{i})\land \forall i\in I(x\not\in B_{i})$.

$x \in\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcap_{i \in I}B_{i} \right)$ is equivalent to $x \in \bigcup_{i \in I}A_{i}\land x\not\in \bigcap_{i \in I}B_{i}$, which is equivalent to 
$\exists i\in I(x \in A_{i})\land \neg \forall i\in I(x \in B_{i})$, which is equivalent to 
$\exists i\in I(x \in A_{i})\land \exists i\in I(x\not\in B_{i})$.

They are not equivalent.

(b)

$A_{2}=\{2,4  \}$, $A_{3}=\{ 3,6 \}$, $B_{2}=\{ 2,3 \}$, $B_{3}=\{ 3,4 \}$.
$A_{2}\setminus B_{2}=\{ 4 \}$, $A_{3}\setminus B_{3}=\{ 6 \}$, $\bigcup_{i \in I}(A_{i}\setminus B_{i})=\{ 4,6 \}$.

$\left( \bigcup_{i \in I}A_{i} \right)=\{ 2,3,4,6 \}$. $\left( \bigcup_{i \in I}B_{i} \right)=\{ 2,3,4 \}$, $\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcup_{i \in I}B_{i} \right)=\{ 6 \}$.

$\bigcap_{i \in I}B_{i}=\{ 3 \}$, $\left( \bigcup_{i \in I}A_{i} \right)\setminus\left( \bigcap_{i \in I}B_{i} \right)=\{ 2,4,6 \}$.

10.Give an example of index set $I$ and indexed families of sets $\{ A_{i}\mid i\in I \}$ and $\{ B_{i}\mid i \in I \}$ such that $\bigcup_{i \in I}(A_{i}\cap B_{i})\neq\left( \bigcup_{i \in I}A_{i} \right)\cap\left( \bigcup_{i \in I}B_{i} \right)$.

$x \in\bigcup_{i \in I}(A_{i}\cap B_{i})$ is equivalent to $\exists i\in I(x \in A_{i}\cap B_{i})$, which is equivalent to 
$\exists i\in I(x \in A_{i}\land x \in B_{i})$.

$x \in\left( \bigcup_{i \in I}A_{i} \right)\cap\left( \bigcup_{i \in I}B_{i} \right)$ is equivalent to $x \in \bigcup_{i \in I}A_{i}\land x \in \bigcup_{i \in I}B_{i}$, which is equivalent to
$\exists i\in I(x \in A_{i})\land \exists i\in I(x \in B_{i})$.

If the set $I$ has only one element called $a$，then $\exists i\in I(x \in A_{i}\land x \in B_{i})$ is equivalent to $x \in A_{a}\land x \in B_{a}$. $\exists i\in I(x \in A_{i})\land \exists i\in I(x \in B_{i})$ is equivalent to $x \in A_{a}\land x \in B_{a}$，The two are equivalent. $\bigcup_{i \in I}(A_{i}\cap B_{i})=\left( \bigcup_{i \in I}A_{i} \right)\cap\left( \bigcup_{i \in I}B_{i} \right)$

Then we need to consider $I=\{ 1,2 \}$. Let $A_{1}=\{ 1,2 \}$, $A_{2}=\emptyset$, $B_{1}=\emptyset$, $B_{2}=\{ 2,3 \}$. In the situation, $\bigcup_{i \in I}(A_{i}\cap B_{i})=\emptyset$, $\left( \bigcup_{i \in I}A_{i} \right)\cap\left( \bigcup_{i \in I}B_{i} \right)=\{ 2 \}$. $\bigcup_{i \in I}(A_{i}\cap B_{i})\neq\left( \bigcup_{i \in I}A_{i} \right)\cap\left( \bigcup_{i \in I}B_{i} \right)$.

11.Show that for any sets $A$ and $B$, $\mathscr{P}(A\cap B)=\mathscr{P}(A)\cap \mathscr{P}(B)$, by showing that the statements $x \in \mathscr{P}(A\cap B)$ and $x \in \mathscr{P}(A)\cap \mathscr{P}(B)$ are equivalent.

$x \in \mathscr{P}(A\cap B)$ is equivalent to $x\subseteq A\cap B$, which is equivalent to
$\forall y(y\in x\to y\in A\cap B)$, which is equivalent to
$\forall y(y\in x\to (y\in A\land y\in B))$.

$x \in \mathscr{P}(A)\cap \mathscr{P}(B)$ is equivalent to $x \in \mathscr{P}(A)\land x \in \mathscr{P}(B)$, which is equivalent to
$x\subseteq A\land x\subseteq B$, which is equivalent to
$\forall y(y\in x\to y\in A)\land \forall y(y\in x\to y \in B)$.

Because of [[1.5 The Conditional and Biconditional Connectives#^equivalence1]] [[2.2 Equivalences involving Quantifiers#^equivalence1]]

We can get $\forall y(y\in x\to(y\in A\land y\in B))$ is equivalent to $\forall y((y\in x\to y\in A)\land (y\in x\to y\in B))$, which is equivalent to 
$\forall y(y\in x\to y\in A)\land \forall y(y\in x\to y\in B)$.

Then $x \in \mathscr{P}(A\cap B)$ is equivalent to $x \in \mathscr{P}(A)\cap \mathscr{P}(B)$.

12.Give examples of sets $A$ and $B$ for which $\mathscr{P}(A\cup B)\neq \mathscr{P}(A)\cup \mathscr{P}(B)$.

$x \in \mathscr{P}(A\cup B)$ is equivalent to $\forall y(y\in x\to y\in A\cup B)$, which is equivalent to
$\forall y(y\in x\to(y\in A\lor y\in B))$.

$x \in \mathscr{P}(A)\cup \mathscr{P}(B)$ is equivalent to $x \in \mathscr{P}(A)\lor x \in \mathscr{P}(B)$, which is equivalent to 
$x\subseteq A\lor x\subseteq B$, which is equivalent to
$\forall y(y\in x\to y\in A)\lor \forall y(y\in x\to y\in B)$.


Because of [[1.5 The Conditional and Biconditional Connectives#^equivalence2]] we get $\forall y(y\in x\to(y\in A\lor y\in B))$ is equivalent to $\forall y((y\in x\to y\in A)\lor(y\in x\to y\in B))$.

We can use the Venn diagram to easily find the counterexample when $A\not\subseteq B$ and $B\not\subseteq A$.

Let $A=\{ 1 \}$, $B=\{ 2 \}$, $A\cup B=\{ 1,2 \}$, $\mathscr{P}(A\cup B)=\{ \emptyset,\{ 1 \},\{ 2 \},\{ 1,2 \} \}$, $\mathscr{P}(A)=\{ \emptyset,\{ 1 \} \}$, $\mathscr{P}(B)=\{ \emptyset,\{ 2 \} \}$. $\mathscr{P}(A)\cup \mathscr{P}(B)=\{\emptyset,\{ 1 \},\{ 2 \} \}$. $\mathscr{P}(A\cup B)\neq \mathscr{P}(A)\cup \mathscr{P}(B)$.

13.Verify the following identities by writing out (using logical symbols) what it means for an object $x$ to be an element of each set and then using logical equivalences.

(a) $\bigcup_{i \in I}(A_{i}\cup B_{i})=\left( \bigcup_{i \in I}A_{i} \right)\cup\left( \bigcup_{i \in I}B_{i} \right)$.
(b) $\left( \bigcap \mathcal{F} \right)\cap\left( \bigcap \mathcal{G} \right)=\bigcap(\mathcal{F}\cup \mathcal{G})$.
(c)  $\bigcap_{i \in I}(A_{i}\setminus B_{i})=\left( \bigcap_{i \in I}A_{i} \right)\setminus\left( \bigcup_{i \in I}B_{i} \right)$.

(a) 
$x \in \bigcup_{i \in I}(A_{i}\cup B_{i})$ is equivalent to $\exists i\in I(x\in A_{i}\cup B_{i})$, which is equivalent to 
$\exists i\in I(x \in A_{i}\lor x \in B_{i})$.

$x \in\left( \bigcup_{i \in I}A_{i} \right)\cup\left( \bigcup_{i \in I}B_{i} \right)$ is equivalent to $x \in\left( \bigcup_{i \in I}A_{i} \right)\lor x \in\left( \bigcup_{i \in I}B_{i} \right)$, which is equivalent to 
$\exists i\in I(x \in A_{i})\lor \exists i\in I(x \in B_{i})$.

pass
$\exists x(P(x)\lor Q(x))$ is equivalent to $\exists x\,P(x)\lor \exists x\,Q(x)$.

$\exists i\in I(x \in A_{i}\lor x \in B_{i})$ is equivalent to $\exists i\in I(x \in A_{i})\lor \exists i\in I(x \in B_{i})$.

(b)
$x \in\left( \bigcap \mathcal{F} \right)\cap\left( \bigcap \mathcal{G} \right)$ is equivalent to $x \in \left( \bigcap \mathcal{F} \right)\land x \in \left( \bigcap \mathcal{G} \right)$, which is equivalent to 
$\forall A\in \mathcal{F}(x \in A)\land \forall A\in \mathcal{G}(x \in A)$.

$x \in \bigcap(\mathcal{F}\cup \mathcal{G})$ is equivalent to $\forall A\in (\mathcal{F}\cup \mathcal{G})(x \in A)$.

pass
$\forall x \in S\,P(x)\land \forall x \in T\,P(x)$ is equivalent to $\forall x \in(S\cup T)\,P(x)$.

(c)
$x \in \bigcap_{i \in I}(A_{i}\setminus B_{i})$ is equivalent to $\forall i\in I(x \in A_{i}\setminus B_{i})$, which is equivalent to 
$\forall i\in I(x \in A_{i}\land x\not\in B_{i})$.

$x \in\left( \bigcap_{i \in I}A_{i} \right)\setminus\left( \bigcup_{i \in I}B_{i} \right)$ is equivalent to $x \in\left( \bigcap_{i \in I}A_{i} \right)\land x\not\in\left( \bigcup_{i \in I}B_{i} \right)$, which is equivalent to 
$\forall i\in I(x \in A_{i})\land \neg \exists i\in I(x \in B_{i})$, which is equivalent to $\forall i\in I(x \in A_{i})\land \forall i\in I(x\not\in B_{i})$.

Because of [[2.2 Equivalences involving Quantifiers#^equivalence1]] we get $\forall x(E(x)\land T(x))$ is equivalent to $\forall xE(x)\land \forall xT(x)$. So $\forall x(E(x)\land \neg T(x))$ is also equivalent to $\forall xE(x)\land \forall x\neg T(x)$.

$\forall i\in I(x \in A_{i}\land x\not\in B_{i})$ is equivalent to $\forall i\in I(x \in A_{i})\land \forall i\in I(x\not\in B_{i})$

14.Sometimes each set in an indexed family of sets has two indices. For this problem, use the following definition: $I=\{ 1,2 \}, J=\{ 3,4 \}$. For each $i\in I$ and $j\in J$, let $A_{i,j}=\{ i,j,i+j \}$. Thus, for example, $A_{2,3}=\{ 2,3,5 \}$.

(a) For each $j\in J$ let $B_{j}=\bigcup_{i \in I}A_{i,j}=A_{1,j}\cup A_{2,j}$. Find $B_{3}$ and $B_{4}$.
(b) Find $\bigcap_{j \in J}B_{j}$. 
(c) Find $\bigcup_{i \in I}\left( \bigcap_{j \in J}A_{i,j} \right)$.
(d) Analyze the logical forms of the statements $x \in \bigcap_{j \in J}\left( \bigcup_{i \in I}A_{i,j} \right)$ and $x \in \bigcup_{i \in I}\left( \bigcap_{j \in J}A_{i,j} \right)$.

(a)
$A_{1,3}=\{ 1,3,4 \}$.
$A_{1,4}=\{ 1,4,5 \}$.
$A_{2,3}=\{ 2,3,5 \}$.
$A_{2,4}=\{ 2,4,6 \}$.

$B_{3}=A_{1,3}\cup A_{2,3}=\{1,2,3,4,5\}$.
$B_{4}=A_{1,4}\cup A_{2,4}=\{ 1,2,4,5,6 \}$.

(b)
$\bigcap_{j \in J}B_{j}=B_{3}\cap B_{4}=\{ 1,2,4,5 \}$.

(c)
Based on the calculation of (a) and (b), let $B_{i}=\bigcap_{j \in J}A_{i,j}=A_{i,3}\cap A_{i,4}$, $B_{1}=A_{1,3}\cap A_{1,4}=\{ 1,4 \}$. $B_{2}=A_{2,3}\cap A_{2,4}=\{ 2 \}$. $\bigcup_{i \in I}\left( \bigcap_{j \in J}A_{i,j} \right)=\bigcup_{i \in I}B_{i}=B_{1}\cup B_{2}=\{ 1,2,4 \}$.

They are not equal.

(d)
$x \in \bigcap_{j \in J}\left( \bigcup_{i \in I}A_{i,j} \right)$ is equivalent to $\forall j\in J\left( x \in\left( \bigcup_{i \in I}A_{i,j} \right) \right)$, which is equivalent to $\forall j\in J\,\exists i\in I(x \in A_{i,j})$.

$x \in \bigcup_{i \in I}\left( \bigcap_{j \in J}A_{i,j} \right)$ is equivalent to $\exists i\in I\left( x \in \bigcap_{j \in J}A_{i,j} \right)=\exists i\in I\,\forall j\in J(x \in A_{i,j})$.

They are not equivalent.

15.
(a) Show that if $\mathcal{F}=\emptyset$, then the statement $x \in \bigcup \mathcal{F}$ will be false no matter what $x$ is. It follows that $\bigcup \emptyset=\emptyset$.
(b) Show that if $\mathcal{F}=\emptyset$, then the statement $x \in \bigcap \mathcal{F}$ will be true no matter what $x$ is. In a context in which it is clear what the universe of discourse $U$ is. we might therefore want to say that $\bigcap \emptyset=U$. However, this has the unfortunate consequence that the notation $\bigcap \emptyset$ will mean different things in different contexts. Furthermore, when working with sets whose elements are sets, mathematicians often do not use a universe of discourse at all. For these reasons, some mathematicians consider the notation $\bigcap \emptyset$ to be meaningless. We will avoid this problem in this book by using the notation $\bigcap \mathcal{F}$ only in contexts in which we can be sure that $\mathcal{F}\neq \emptyset$.


(a)
$x \in \bigcup \mathcal{F}$ is equivalent to $\exists A\in \mathcal{F}(x \in A)$. If $\mathcal{F}=\emptyset$, we can't find $A\in \mathcal{F}$ because no element is in $\emptyset$, so $x \in \bigcup \mathcal{F}$ is false for any $x$.

(b)

$x \in \bigcap \mathcal{F}$ is equivalent to $\forall A\in \mathcal{F}(x \in A)$, which is equivalent to $\neg \exists A\in \mathcal{F}(x\not\in A)$. If $\mathcal{F}=\emptyset$, $\exists A\in \mathcal{F}(x\not\in A)$ is false for any $x$ for the reason above, so $x \in \bigcap \mathcal{F}$ is true for any $x$. We get $\bigcap \mathcal{F}=U$.

16.In section 2.3 we saw that a set can have other sets as elements. When discussing sets whose elements are sets, it might seem most natrual to consider the universe of discourse to be the set of all sets. However, as we will see in this problem, assuming that there is such a set leads to contradiction.

Suppose $U$ were the set of all sets. Note that in particular $U$ is a set, so we would have $U\in U$. This is not yet a contraction; although most sets are not elements of themselves, perhaps some sets are elements of themselves. But it sugguests that the sets in the universe $U$ could be split into two categories: the unusual sets that, like $U$ itself, are elements of themselves, and the more typical sets that are not. Let $R$ be the set of sets in the second category. In other words, $R=\{ A\in U\mid A\not\in A \}$. This means that for any set $A$ in the universe $U$, $A$ will be an element of $R$ iff $A\not\in A$. In other words, we have $\forall A\in U(A\in R\leftrightarrow A\not\in A)$.

(a) Show that applying this last fact to the set $R$ itself (in other words, plugging in $R$ for $A$) leads to a contraction. This contradiction was discovered by Bertrand Russell (1872–1970) in 1901, and is known as Russell’s paradox.
(b) Think some more about the paradox in part (a). What do you think it tell us about sets?

(a)
From $\forall A\in U(A\in R\leftrightarrow A\not\in A)$, because $R\in U$, $R\in R\leftrightarrow R\not\in R$ is true, but that is impossible.

(b)
Therefore the assumption that there exists a set UUU containing all sets leads to a contradiction. Hence there cannot exist a set of all sets.

**There is no set containing all sets.**