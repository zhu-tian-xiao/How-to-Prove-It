$\exists!xP(x)$: There is exactly one $x$ such that $P(x)$. It can be thought of as an abbreviation for the formula $\exists x(P(x)\land \neg\exists y(P(y)\land y\neq x))$.

$\neg \exists y(P(y)\land y\neq x)$ is equivalent to $\forall y\neg(P(y)\land y\neq x)$, 
which is equivalent to $\forall y(\neg P(y)\lor y=x)$, 
which is equivalent to $\forall y(P(y)\to y=x)$.

Theorem. The following are equivalent: 
1.$\exists x(P(x)\land \forall y(P(y)\to y=x))$.
2.$\exists x\forall y(P(y)\leftrightarrow y=x)$.
3.$\exists xP(x)\land \forall y\forall z((P(y)\land P(z))\to y=z)$.

Proof. 1 $\to$ 2. By statement 1, we can let $x_{0}$ be some object such that $P(x_{0})$ and $\forall y(P(y)\to y=x_{0})$. To prove statement 2 we will show that $\forall y(P(y)\leftrightarrow y=x_{0})$. Let $y$ be arbitrary. We already know the $\to$ direction of the biconditional. For the $\leftarrow$ direction, suppose $y=x_{0}$. The since we know $P(x_{0})$, we can conclude $P(y)$. 

2 $\to$ 3. By statement 2, choose $x_{0}$ such that $\forall y(P(y)\leftrightarrow y=x_{0})$. Then, in particular, $P(x_{0})\leftrightarrow x_{0}=x_{0}$, and since clearly $x_{0}=x_{0}$, it follows that $P(x_{0})$ is true. Thus, $\exists xP(x)$. To prove the second half of statement 3, let $y$ and $z$ be arbitrary and suppose $P(y)$ and $P(z)$. Then by our choice of $x_{0}$, it follows that $y=x_{0}$ and $z=x_{0}$, so $y=z$.

3 $\to$ 1. By the first half of statement 3, let $x_{0}$ be some object such that $P(x_{0})$. Statement 1 will follow if we can show that $\forall y(P(y)\to y=x_{0})$, so suppose $P(y)$. Since we now have both $P(x_{0})$ and $P(y)$, by the second half of statement 3 we can conclude that $y=x_{0}$, as required.

To prove a goal of the form $\exists !xP(x)$:

Prove $\exists xP(x)$ and $\forall y\forall z(P(y)\land P(z)\to y=z)$. The first of these goals shows that there exists an $x$ such that $P(x)$ is true, and the second shows that it is unique. The two parts of the proof are therefore sometimes labeled existence and uniqueness. 

Form of final proof:

$$
\begin{align}
&\text{Existence:[Proof of }\exists xP(x)\text{ goes here.} \\
&\text{Uniqueness:[Proof of }\forall y\forall z(P(y)\land P(z)\to y=z)\text{ goes here.} 
\end{align}
$$

Theorem. There is a unique set $A$ such that for every set $B$, $A\cup B=B$.

Proof. Existence: Clearly $\forall B(\emptyset \cup B=B)$, so $\emptyset$ has the required property.

Uniqueness: Suppose $\forall B(C\cup B)=B$ and $\forall B(D\cup B=B)$. Applying the first of these assumptions to $D$ we see that $C\cup D=D$, and applying the second to $C$ we get $D\cup C=C$. But clearly $C\cup D=D\cup C$, so $C=D$.

To prove a goal of the form $\exists!xP(x)$:

Prove $\exists x(P(x)\land \forall y(P(y)\to y=x))$, using strategies from previous sections.

Theorem. For every real number $x$, if $x\neq 2$ then there is a unique real number $y$ such that $\frac{2y}{y+1}=x$.

Proof. Let $x$ be an arbitrary real number, and suppose $x\neq 2$. Let $y=\frac{x}{2-x}$, which is defined since $x\neq 2$. Then 
$$
\frac{2y}{y+1}=\frac{\frac{2x}{2-x}}{\frac{x}{2-x}+1}=x\text{.}
$$
To see that this solution is unique, suppose $\frac{2z}{z+1}=x$. Since $x\neq 2$, $z=\frac{x}{2-x}=y$.

To use a given of the form $\exists!xP(x)$:

Treat this as two given statement, $\exists xP(x)$ and $\forall y\forall z((P(y)\land P(z))\to y=z)$. To use the first statement you should probably choose a name, say $x_{0}$, to stand for some object such that $P(x_{0})$ is true. The second tells you that if you ever come across two objects $y$ and $z$ such that $P(y)$ and $P(z)$ are both true, you can conclude that $y=z$.

Theorem. Suppose $A$, $B$ and $C$ are sets, $A$ and $B$ are not disjoint, $A$ and $C$ are not disjoint, and $A$ has exactly one element. Then $B$ and $C$ are not disjoint.

Proof. Since $A$ and $B$ are not disjoint, we can let $b$ be something such that $b\in A$ and $b\in B$. Similarly, since $A$ and $C$ are not disjoint, there is some object $c$ such that $c\in A$ and $c\in C$. Since $A$ has only one element, we must have $b=c$. Thus $b=c\in B\cap C$ and therefore $B$ and $C$ are not disjoint.

# Exercises
1.Prove that for every real number $x$ there is a unique real number $y$ such that $x^2y=x-y$.

Let $x$ be an arbitrary real number. Let $y=\frac{x}{x^2+1}$. Then 

$$
x-y=x-\frac{x}{x^2+1}=\frac{x^3}{x^2+1}=x^2\cdot y\text{.}
$$
To see that $y$ is unique, suppose that $x^2z=x-z$. Then $z(x^2+1)=x$, and since $x^2+1\neq 0$, we can divide both sides by $x^2+1$ to conclude that $z=\frac{x}{x^2+1}=y$.

2.Prove that there is a unique real number $x$ such that for every real number $y$, $xy+x-4=4y$.

Let $x=4$. Let $y$ be arbitrary. $xy+x-4=4y$. 

To see that $x$ is unique, suppose that for every real number $y$, $zy+z-4=4y$. Let $y=0$. $z=4=x$. 

3.Prove that for every real number $x$, if $x\neq 0$ and $x\neq 1$ then there is a unique real number $y$ such that $\frac{y}{x}=y-x$.

Suppose $x\neq 0$ and $x\neq 1$. Since $x\neq 1$, let $y=\frac{x^2}{x-1}$. $y-x=\frac{x^2}{x-1}-x=\frac{x}{x-1}$. Since $x\neq 0$, $\frac{x}{x-1}=\frac{x^2}{x-1}\cdot \frac{1}{x}=y\cdot \frac{1}{x}=\frac{y}{x}$.

To prove that $y$ is unique, suppose $\frac{z}{x}=z-x$. Since $x\neq 1$, $z=\frac{x^2}{x-1}=y$.

4.Prove that for every real number $x$, if $x\neq 0$ then there is a unique number $y$ such that for every real number $z$, $zy=\frac{z}{x}$.

Suppose $x\neq 0$. Let $y=\frac{1}{x}$. Now let $z$ be an arbitrary real number. Then $zy=z\left( \frac{1}{x} \right)=\frac{z}{x}$, as required.

To see  that $y$ is unique, suppose that $y'$ is a number  with the property that $\forall z\in \mathbb{R}\left( zy'=\frac{z}{x} \right)$. Then in particular, taking $z=1$, we have $y'=\frac{1}{x}$, so $y'=y$.

5.Recall that if $\mathcal{F}$ is a family of sets, then $\bigcup \mathcal{F}=\{ x\mid\exists A(A\in \mathcal{F}\land x \in A) \}$. Suppose we define a new set $\bigcup!\mathcal{F}$ by the formula $\bigcup!\mathcal{F}=\{ x\mid \exists!A(A\in \mathcal{F}\land x \in A) \}$. 

(a) Prove that for any family of sets $\mathcal{F}$, $\bigcup!\mathcal{F}\subseteq \bigcup \mathcal{F}$.

(b) A family of sets $\mathcal{F}$ is said to be pairwise disjoint if every pair of distinct elements of $\mathcal{F}$ are disjoint; that is , $\forall A\in \mathcal{F}\forall B\in \mathcal{F}(A\neq B\to A\cap B=\emptyset)$. Prove that for any family of sets $\mathcal{F}$, $\bigcup!\mathcal{F}=\bigcup \mathcal{F}$. iff $\mathcal{F}$ is pairwise disjoint.

(a) Suppose $x \in \bigcup!\mathcal{F}$. $\exists !A(A\in \mathcal{F}\land x \in A)$. Then we can choose some $A\in \mathcal{F}$ and $x \in A$. Thus, $x \in \bigcup \mathcal{F}$.

(b) 

($\to$) Let $\mathcal{F}$ be arbitrary. $\bigcup!\mathcal{F}=\bigcup \mathcal{F}$. Suppose $A\in \mathcal{F}$, $B\in \mathcal{F}$ be arbitrary and $A\neq B$. Suppose $A\cap B\neq \emptyset$. Then there is some $y\in A\cap B$. $y\in A$ and $y\in B$. Since $A\in \mathcal{F}$ and $y\in A$, $y\in \bigcup \mathcal{F}$. Since $\bigcup!\mathcal{F}=\bigcup \mathcal{F}$, $y\in \bigcup!\mathcal{F}$. Then $\exists !A(A\in \mathcal{F}\land y\in A)$. Since $A\in \mathcal{F}\land y\in A$ and $B\in \mathcal{F}\land y\in B$ and $A\neq B$, there is a contradiction. Thus, $A\cap B= \emptyset$. Therefore, $\forall A\in \mathcal{F}\forall B\in \mathcal{F}(A\neq B\to A\cap B=\emptyset)$.

($\leftarrow$) Suppose $\mathcal{F}$ is pairwise disjoint. $\forall A\in \mathcal{F}\forall B\in \mathcal{F}(A\neq B\to A\cap B=\emptyset)$. We have proven that $\bigcup!\mathcal{F}\subseteq \bigcup \mathcal{F}$. Next, we need to prove $\bigcup \mathcal{F}\subseteq \bigcup!\mathcal{F}$. Suppose $x \in \bigcup \mathcal{F}$. Then there is some $A\in \mathcal{F}$ such that $x \in A$. So $\exists A(A\in \mathcal{F}\land x \in A)$. Suppose $B\in \mathcal{F}\land x \in B$ and $C\in \mathcal{F}\land x \in C$. Since $\forall A\in \mathcal{F}\forall B\in \mathcal{F}(A\neq B\to A\cap B=\emptyset)$, it follows that $B\neq C\to B\cap C=\emptyset$. Since $x \in B$ and $x \in C$, $x \in B\cap C$. So $B\cap C\neq \emptyset$. Then $B=C$. Then $\exists !A(A\in \mathcal{F}\land x \in A)$. $x \in \bigcup!\mathcal{F}$.

6.Let $U$ be any sets.
(a) Prove that there is a unique $A\in \mathscr{P}(U)$ such that for every $B\in \mathscr{P}(U)$, $A\cup B=B$.

(b) Prove that there is a unique $A\in \mathscr{P}(U)$ such that for every $B\in \mathscr{P}(U)$, $A\cup B=A$.

(a)

Let $A=\emptyset \in \mathscr{P}(U)$. Then clearly for any $B\in \mathscr{P}(U)$, $A\cup B=\emptyset \cup B=B$.

To see that $A$ is unique, suppose that $A' \in \mathscr{P}(U)$ and for all $B\in \mathscr{P}(U)$, $A'\cup B=B$. Then in particular, taking $B=\emptyset$, we can conclude that $A'\cup \emptyset=\emptyset$. We have $A'=\emptyset=A$.

(b)

Let $A=U\in \mathscr{P}(U)$. Then clearly for any $B\in \mathscr{P}(U)$, $A\cup B=U\cup B=U=A$.

To see that $A$ is unique, suppose $A'\in \mathscr{P}(U)$ and for all $B\in \mathscr{P}(U)$, $A'\cup B=A'$. Then in particular, taking $B=U$, we can conclude that $A'\cup B=A'\cup U=U=A'$. We have $A'=A$.

7.Let $U$ be any set.

(a) Prove that there is a unique $A\in \mathscr{P}(U)$ such that for every $B\in \mathscr{P}(U)$, $A\cap B=B$.

(b) Prove that there is a unique $A\in \mathscr{P}(U)$ such that for every $B\in \mathscr{P}(U)$, $A\cap B=A$.

(a) Let $A=U\in \mathscr{P}(U)$. Then for every $B\in \mathscr{P}(U)$, $A\cap B=U\cap B=B$.

To see that $A$ is unique, suppose $A'\in \mathscr{P}(U)$ and for all $B\in \mathscr{P}(U)$, $A'\cap B=B$. Taking $B=U$, we can conclude that $A'\cap U=A'=U=A$.

(b) Let $A=\emptyset \in \mathscr{P}(U)$. Then for every $B\in \mathscr{P}(U)$, $A\cap B=\emptyset \cap B=\emptyset=A$.

To see that $A$ is unique, suppose $A'\in \mathscr{P}(U)$ and for all $B\in \mathscr{P}(U)$, $A'\cap B=A'$. Taking $B=\emptyset$, we can conclude that $A'\cap \emptyset=\emptyset=A'$. So $A'=\emptyset=A$.

8.Let $U$ be any set.

(a) Prove that for every $A\in \mathscr{P}(U)$ there is a unique $B\in \mathscr{P}(U)$ such that for every $C\in \mathscr{P}(U)$, $C\setminus A=C\cap B$.

(b) Prove that for every $A\in \mathscr{P}(U)$ there is a unique $B\in \mathscr{P}(U)$ such that for every $C\in \mathscr{P}(U)$, $C\cap A=C\setminus B$.

(a) 

Let $A\in \mathscr{P}(U)$ was arbitrary. Let $B=U\setminus A$. Let $C\in \mathscr{P}(U)$ was arbitrary. 

($\to$) Suppose $x \in C\setminus A$. $x \in C$ and $x\not\in A$. Since $x \in U$, $x \in U\setminus A$. Since $x \in C$, $x \in C\cap(U\setminus A)$. $x \in C\cap B$.

($\leftarrow$) Suppose $x \in C\cap B$. Then $x \in C\cap(U\setminus A)$. Thus, $x \in C$ and $x \in U\setminus A$. Then $x\not\in A$. So $x \in C\setminus A$.

To see that $B$ is unique, suppose there is some $D\in \mathscr{P}(U)$ such that for every $C\in \mathscr{P}(U)$, $C\setminus A=C\cap D$. Suppose there is some $E\in \mathscr{P}(U)$ such that for every $C\in \mathscr{P}(U)$, $C\setminus A=C\cap E$. For the 2 assumptions, take $C=U$, then we conclude that $U\setminus A=U\cap D=D$ and $U\setminus A=U\cap E=E$. Thus, $D=E$.

(b) 

$B=U\setminus A$. The proof is similar to (a).

To see that $B$ is unique, suppose there is some $D\in \mathscr{P}(U)$ such that for every $C\in \mathscr{P}(U)$, $C\cap A=C\setminus D$. Suppose there is some $E\in \mathscr{P}(U)$ such that for every $C\in \mathscr{P}(U)$, $C\cap A=C\setminus E$. Taking $C=U$ for the two assumptions, we can conclude that $U\cap A=U\setminus D$ and $U\cap A=U\setminus E$. Then $U\setminus D=U\setminus E$. Next, we will prove that $D=E$.

($\to$) Suppose $x \in D$. Since $D\in \mathscr{P}(U)$, $D\subseteq U$. Then we know $x \in U$. Suppose $x\not\in E$. Then $x \in U\setminus E$. Since $U\setminus D=U\setminus E$, $x \in U\setminus D$. Then $x\not\in D$. Then there is a contradiction. Thus, $x \in E$.

($\leftarrow$) The proof is similar.

Therefore, $D=E$.

9.Recall that you showed in exercise 14 of Section 1.4 that symmetric difference is associative; in other words, for all sets $A$, $B$, and $C$, $A\triangle (B\triangle C)=(A\triangle B)\triangle C$. You may also find it useful in this problem to note that symmetric difference is clearly commutitive; in other words, for all sets $A$ and $B$, $A\triangle B=B\triangle A$.

(a) Prove that there is a unique indentity element for symmetric difference. In other words, there is a unique set $X$ such that for every set $A$, $A\triangle X=A$.

(b) Prove that every set has unique inverse for the operation of symmetric difference. In other words, for every set $A$ there is a unique set $B$ such that $A\triangle B=X$, where $X$ is the identity element from part (a).

(c) Prove that for any sets $A$ and $B$ there is a unique set $C$ such that $A\triangle C=B$.

(d) Prove that for every set $A$ there is a unique set $B\subseteq A$ such that for every set $C\subseteq A$, $B\triangle C=A\setminus C$.

(a) 

Let $X=\emptyset$. Suppose $A$ was arbitrary. $A\triangle X=A\triangle \emptyset$.

We can prove $A\triangle \emptyset=A$. Suppose $x$ was arbitrary. $x \in A\triangle \emptyset$ iff $(x \in A\land x\not\in \emptyset)\lor(x\not\in A\land x \in \emptyset)$ iff $x \in A$.

To see $X=\emptyset$ is unique, suppose there is some $Y$ such that $\forall A(A\triangle Y=A)$. Taking $A=\emptyset$, we can conclude that $\emptyset \triangle Y=Y=\emptyset=X$.

(b) Suppose $A$ was arbitrary. Let $B=A$. We will prove $A\triangle A=\emptyset$.

Suppose $A\triangle A\neq \emptyset$. Then we can choose some $x \in A\triangle A$. Then $x \in A\cap x\not\in A$, which leads to a contradiction. Thus, $A\triangle A=\emptyset$.

To see $B=A$ is unique, suppose there is some $C$ such that $A\triangle C=\emptyset$. Then we will prove $A=C$.

Suppose $x \in A$. Suppose $x\not\in C$. Then we can conclude $x \in A\triangle C$. Since $A\triangle C=\emptyset$,  $x \in \emptyset$. This leads to a contradiction. Thus, $x \in C$. Then we prove that $A\subseteq C$.

The proof of $C\subseteq A$ is similar.

Thus, $A=C$.

(c) Suppose $A$ and $B$ was arbitrary. Let $C=A\triangle B$. $A\triangle C=A\triangle(A\triangle B)$. Since symmetric difference is associative, $A\triangle (A\triangle B)=(A\triangle A)\triangle B=\emptyset \triangle B=B$.

To see $C=A\triangle B$ is unique, suppose there is some $D$ such that $A\triangle D=B$. Then $A\triangle B=A\triangle(A\triangle D)=(A\triangle A)\triangle D=\emptyset \triangle D=D$. Thus, $D=C$. 

(d) Suppose $A$ was arbitrary. Let $B=A$. Suppose $C$ was arbitrary. We will prove $A\triangle C=A\setminus C$.

($\leftarrow$) The proof is simple.

($\to$) Suppose $x \in A\triangle C$. $x \in A\setminus C$ or $x \in C\setminus A$. Suppose $x \in C\setminus A$. $x \in C$ and $x\not\in A$. Since $C\subseteq A$, $x \in A$. This will lead to a contradiction. Thus, $x \in A\setminus C$.

To see $B=A$ is unique, suppose there is some $D$ such that for every set $C\subseteq A$, $D\triangle C=A\setminus C$. Taking $C=A\subseteq A$. $D\triangle C=D\triangle A=A\setminus A$. Next, we will prove $A\setminus A=\emptyset$.

Suppose $A\setminus A\neq \emptyset$. Then there is some $x \in A\setminus A$. $x \in A$ and $x\not\in A$. This leads to a contradiction. Thus, $A\setminus A=\emptyset$.

Then $D\triangle A=\emptyset$. Thus, $(D\triangle A)\triangle A=\emptyset  \triangle A=A$. Then, $A=D\triangle(A\triangle A)=D\triangle \emptyset=D$.

10.Suppose $A$ is a set, and for every family of sets $\mathcal{F}$, if $\bigcup \mathcal{F}=A$ then $A\in \mathcal{F}$. Prove that $A$ has exactly one element.

We have $A=\emptyset$ or $A$ has exactly one element or $A$ has at least two elements.

Suppose $A=\emptyset$. We have $\forall \mathcal{F}\left( \bigcup \mathcal{F}=A\to A\in \mathcal{F} \right)$. Suppose $\mathcal{F}$ was arbitrary. Then $\bigcup \mathcal{F}=A\to A\in \mathcal{F}$. Taking $\mathcal{F}=\emptyset$, $\bigcup \mathcal{F}=\bigcup \emptyset=\emptyset=A$. Thus, $A=\emptyset \in \emptyset$, which leads to a contradiction.

Suppose $A$ has at least two elements. Then we can choose $a\in A$ and $b\in A$ and $a\neq b$. Then we can take $\mathcal{F}=\{ \{ a \}, \{ b \}, (A\setminus \{ a, b \}) \}$. Obviously, $\bigcup \mathcal{F}=A$. Then $A\in \mathcal{F}$. We know $A=\{ a, b \}\cup(A\setminus \{ a,b \})$. Then $A\neq \{ a \}$ and $A\neq \{ b \}$ and $A\neq(A\setminus \{ a,b \})$. Thus, $A\not\in \mathcal{F}$. This leads to a contradiction.

Therefore, $A$ has exactly one element.


11.Suppose $\mathcal{F}$ is a family of sets that has the property that for every $\mathcal{G}\subseteq \mathcal{F}$, $\bigcup \mathcal{G}\in \mathcal{F}$. Prove that there is a unique set $A$ such that $A\in \mathcal{F}$ and $\forall B\in \mathcal{F}(B\subseteq A)$.

Existence: We given that for every $\mathcal{G}\subseteq \mathcal{F}$, $\bigcup \mathcal{G}\in \mathcal{F}$, so in particular, since $\mathcal{F}\subseteq \mathcal{F}$, $\bigcup \mathcal{F}\in \mathcal{F}$. Let $A=\bigcup \mathcal{F}$. Now suppose $B\in \mathcal{F}$. Then by exercise 8 of Section 3.3, $B\subseteq \bigcup \mathcal{F}=A$, as required.

Uniqueness: Suppose that $A_{1}\in \mathcal{F}$, $A_{2}\in \mathcal{F}$, $\forall B\in \mathcal{F}(B\subseteq A_{1})$, and $\forall B\in \mathcal{F}(B\subseteq A_{2})$. Applying this last fact with $B=A_{1}$ we can conclude that $A_{1}\subseteq A_{2}$, and similarly the previous fact implies that $A_{2}\subseteq A_{1}$. Thus $A_{1}=A_{2}$.

12.(a) Suppose $P(x)$ is a statement with a free variable $x$, Find a formula, using the logical symbols we have studied, that means "there are exactly two values of $x$ for which $P(x)$ is true."

(b) Based on your answer to part (a), design a proof strategy for proving a statement of the form "there are exactly two values for which $P(x)$ is true."

(a) $\exists a(P(a)\land \exists b(P(b)\land b\neq a\land \forall x(P(x)\to(x=a\lor x=b))))$.

(b) First we need to find two different values $a$, $b$ to make $P(a)$ and $P(b)$ be true. Then we can suppose there is some arbitrary $x$ such that $P(x)$ is true. Next, we need to prove that $x=a$ or $x=b$.

13.(a) Prove that there is a unique real number $c$ such that there is a unique real number $x$ such that $x^2+3x+c=0$. (In other words, there is a unique real number $c$ such that the equation $x^2+3x+c=0$ has exactly one solution.)

(b) Show that it is not the case that there is a unique real number $x$ such that there is a unique real number $c$ such that $x^2+3x+c=0$. (Hint: You should be able to prove that for every real number $x$ there is a unique real number $c$ such that $x^2+3x+c=0$.)

(a) Let $c=\frac{9}{4}$. Then $x^2+3x+\frac{9}{4}=0$. It is easy to prove $x=-\frac{3}{2}$ is a solution. To see the solution is unique, suppose there is some $a$ such that $a^2+3a+\frac{9}{4}=0$. $\left( a+\frac{3}{2} \right)^2=0$. Then $a=-\frac{3}{2}=x$.

(b) Suppose $x$ was arbitrary. Let $c=-x^2-3x$. $x^2+3x+c=0$. So $c$ is a solution. To see $c$ is unique, suppose there is some $d$ such that $x^2+3x+d=0$. Thus, $d=-x^2-3x$.