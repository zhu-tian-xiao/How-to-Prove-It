To use a given of the form $P\lor Q$:

Break your proof into cases.

Scratch work

Before using strategy:


| Givens    | Goal |
| --------- | ---- |
| $P\lor Q$ | -    |
| -         |      |

After using strategy:

case1:


| Givens | Goal |
| ------ | ---- |
| $P$    | -    |
| -      |      |
case2:


| Givens | Goal |
| ------ | ---- |
| $Q$    | -    |
| -      |      |

Form of final proof:
$$
\begin{align}
&\text{Case 1. }P\text{ is true.} \\
&\quad\text{[Proof of goal goes here.]} \\
&\text{Case 2. }Q\text{ is true.} \\
&\quad\text{[Proof of goal goes here.]} \\
&\text{Since we know }P\lor Q\text{, these cases cover all the possibilities. Therefore the goal must be true.}
\end{align}
$$

Theorem. Suppose that $A$, $B$, and $C$ are sets. If $A\subseteq C$ and $B\subseteq C$ then $A\cup B\subseteq C$.

Proof. Suppose $A\subseteq C$ and $B\subseteq C$, and let $x$ be an arbitrary element of $A\cup B$. Then either $x \in A$ or $x \in B$.

Case 1. $x \in A$. Then since $A\subseteq C$, $x \in C$.
Case 2. $x \in B$. Then since $B\subseteq C$, $x \in C$.

Since we know that either $x \in A$ or $x \in B$, these cases cover all the possibilities, so we can conclude that $x \in C$. Since $x$ was an arbitrary element of $A\cup B$, this means that $A\cup B\subseteq C$.

To prove a goal of the form $P\lor Q$:

Break your proof into cases. In each case, either prove $P$ or prove $Q$.

Theorem. Suppose that $A$, $B$, and $C$ are sets. Then $A\setminus(B\setminus C)\subseteq(A\setminus B)\cup C$.

Proof. Suppose $x \in A\setminus(B\setminus C)$. Then $x \in A$ and $x\not\in B\setminus C$. Since $x\not\in B\setminus C$, it follows that either $x\not\in B$ or $x \in C$. We will consider these cases separately.

Case 1. $x\not\in B$. Then since $x \in A$, $x \in A\setminus B$, so $x \in(A\setminus B)\cup C$.

Case 2. $x \in C$. Then clearly $x \in(A\setminus B)\cup C$.

Since $x$ was an arbitrary element of $A\setminus(B\setminus C)$, we can conclude that $A\setminus(B\setminus C)\subseteq(A\setminus B)\cup C$.

Theorem. For every integer $x$, the remainder when $x^2$ is divided by $4$ is either $0$ or $1$.

Proof. Suppose $x$ is an integer. We consider two cases.

Case1. $x$ is even. Then $x=2k$ for some integer $k$, so $x^2=4k^2$. Clearly the remainder when $x^2$ is divided by 4 is $0$.

Case 2. $x$ is odd. Then $x=2k+1$ for some integer $k$, so $x^2=4(k^2+k)+1$. Clearly in this case the remainder when $x^2$ is divided by $4$ is $1$.

To prove a goal of the form $P\lor Q$:

If $P$ is true, then clearly the goal $P\lor Q$ is true, so you only need to worry about the case in which $P$ is false. You can complete the proof in this case by proving that $Q$ is true.

Scratch work

Before using strategy:


| Givens | Goal      |
| ------ | --------- |
| -      | $P\lor Q$ |
| -      |           |

After using strategy:


| Givens   | Goal |
| -------- | ---- |
| -        | $Q$  |
| -        |      |
| $\neg P$ |      |

Form of final proof:

$$
\begin{align}
&\text{If }P \text{ is true, then of course }P\lor Q \text{ is true. Now suppose }P\text{ is false.} \\
&\quad\text{[Proof of }Q\text{ goes here.]} \\
&\text{Thus, }P\lor Q\text{ is true.}
\end{align}

$$

Theorem. For every real number $x$, if $x^2\geq x$ then either $x\leq 0$ or $x\geq 1$.

Proof. Suppose $x^2\geq x$. If $x\leq 0$, then of course $x\leq 0$ or $x\geq 1$. Now suppose $x>0$. Then we can divide both sides of the inequality $x^2\geq x$ by $x$ to conclude that $x\geq 1$. Thus, either $x\leq 0$ or $x\geq 1$.

The equivalence of $P\lor Q$ and $\neg P\to Q$ also suggests a rule of inference called **disjunctive syllogism** for using a given  statement of the form $P\lor Q$.

To use a given of the form $P\lor Q$:

If you are also given $\neg P$, or you can prove that $P$ is false, then you can use this given to conclude that $Q$ is true. Similarly, if you are given $\neg Q$ or can prove that $Q$ is false, then you can conclude that $P$ is true.

Theorem 3.5.5. Suppose $m$ and $n$ are integers. If $mn$ is even, then either $m$ is even or $n$ is even.

Proof. Suppose $mn$ is even. Then we can choose an integer $k$ such that $mn=2k$. If $m$ is even then there is nothing more to prove, so suppose $m$ is odd. Then $m=2j+1$ for some integer $j$. Substituting this into the equation $mn=2k$, we get $(2j+1)n=2k$, so $2jn+n=2k$, and therefore $n=2k-2jn=2(k-jn)$. Since $k-jn$ is an integer, it follows that $n$ is even.

# Exercise
1.Suppose $A$, $B$, and $C$ are sets. Prove that $A\cap(B\cup C)\subseteq(A\cap B)\cup C$.

Suppose $x \in A\cap(B\cup C)$. Then $x \in A$, and either $x \in B$ or $x \in C$.

Case 1. $x \in B$. Then since $x \in A$, $x \in A\cap B$, so $x \in(A\cap B)\cup C$.

Case 2. $x \in C$. Then clearly $x \in(A\cap B)\cup C$.

Since $x$ was arbitrary, we can conclude that $A\cap(B\cup C)\subseteq(A\cap B)\cup C$.

2.Suppose $A$, $B$, and $C$ are sets. Prove that $(A\cup B)\setminus C\subseteq A\cup(B\setminus C)$.

Suppose $x \in(A\cup B)\setminus C$. Then $x \in A\cup B$ and $x\not\in C$. Either $x \in A$ or $x \in B$.

Case 1. $x \in A$. Then clearly $x \in A\cup(B\setminus C)$.

Case 2. $x \in B$. Then since $x\not\in C$, $x \in B\setminus C$. Then $x \in A\cup(B\setminus C)$.

Since $x$ was arbitrary, we can conclude that $(A\cup B)\setminus C\subseteq A\cup(B\setminus C)$.

3.Suppose $A$ and $B$ are sets. Prove that $A\setminus(A\setminus B)=A\cap B$.

Supose $x \in A\setminus(A\setminus B)$. Then $x \in A$ and $x\not\in A\setminus B$. $x\not\in A\setminus B$ is equivalent to $x \in A\to x \in B$. It follows that $x \in B$. Thus, $x \in A$ and $x \in B$. $x \in A\cap B$.

Suppose $x \in A\cap B$. Then $x \in A$ and $x \in B$. Then $x\not\in$

5.Suppose $A\cap C\subseteq B\cap C$ and $A\cup C\subseteq B\cup C$. Prove that $A\subseteq B$.

Suppose $x \in A$. We now consider two cases:

Case 1. $x \in C$. Then $x \in A\cap C$, so since $A\cap C\subseteq B\cap C$, $x \in B\cap C$, and therefore $x \in B$.

Case 2. $x\not\in C$. Since $x \in A$, $x \in A\cup C$, so since $A\cup C\subseteq B\cup C$, $x \in B\cup C$. But $x\not\in C$, so we must have $x \in B$.

Thus, $x \in B$, and since $x$ was arbitrary, $A\subseteq B$.


8.Prove that for any sets $A$ and $B$, $\mathscr{P}(A)\cup \mathscr{P}(B)\subseteq \mathscr{P}(A\cup B)$.

Suppose $x \in \mathscr{P}(A)\cup \mathscr{P}(B)$. $x \in \mathscr{P}(A)$ or $x \in \mathscr{P}(B)$.

Case 1. $x \in \mathscr{P}(A)$. Then $x\subseteq A$. Since $A\subseteq A\cup B$, $x\subseteq A\cup B$. Thus, $x \in \mathscr{P}(A\cup B)$.

Case 2. $x \in \mathscr{P}(B)$. Then $x\subseteq B$. Since $B\subseteq A\cup B$, $x\subseteq A\cup B$. Thus, $x \in \mathscr{P}(A\cup B)$.

Since $x$ was arbitrary, we can conclude that $x \in \mathscr{P}(A\cup B)$.

11.Prove that for every real number $x$, if $|x-3|>3$, then $x^2>6x$. 

Suppose $\lvert x-3\rvert>3$.

Case 1. $x-3\geq 0$.  $\lvert x-3 \rvert=x-3>3$. Then $x>6$. Then we can multiply both sides of the inequality $x>6$ by $x$. Thus, $x^2>6x$.

Case 2. $x-3<0$. $\lvert x-3 \rvert =3-x>3$. $x<0$. Then $x<6$. We can multiply both sides of the inequality $x<6$ by $x$. Thus, $x^2>6x$.

Since $x$ was arbitrary, we can conclude that $x^2>6x$.

12.Prove that for every real number $x$, $\lvert 2x-6 \rvert>x$ iff $\lvert x-4 \rvert>2$.

($\to$) Suppose $\lvert 2x-6 \rvert>x$.

Case 1. $2x-6\geq 0$. Then $\lvert 2x-6 \rvert=2x-6>x$. $x>6$. $\lvert x-4 \rvert=x-4>2$.

Case 2. $2x-6<0$. Then $\lvert 2x-6 \rvert=6-2x>x$. $x<2$. Thus, $-x>-2$. Then $\lvert x-4 \rvert=4-x>2$.

($\leftarrow$) Suppose $\lvert x-4 \rvert>2$.

Case 1. $x-4\geq 0$. Then $\lvert x-4 \rvert=x-4$, so we have $x-4>2$, and therefore $x>6$. Adding $x$ to both sides gives us $2x>6+x$, so $2x-6>x$. Since $x>6$, this implies that $2x-6$ is positive, so $\lvert 2x-6 \rvert=2x-6>x$.

Case 2. $x-4<0$. Then $\lvert x-4 \rvert=4-x$, so we have $4-x>2$, and therefore $x<2$. Therefore $3x<6$, and subtracting $2x$ from both sides we get $x<6-2x$. Also, from $x<2$ we get $2x<4$, so $2x-6<-2$. Therefore, $2x-6$ is negative, so $\lvert 2x-6 \rvert=6-2x>x$.

16.Suppose $\mathcal{F}$ and $\mathcal{G}$ are nonempty families of sets.
(a) Prove that $\bigcup(\mathcal{F}\cup \mathcal{G})=\left( \bigcup \mathcal{F} \right)\cup\left( \bigcup \mathcal{G} \right)$.
(b) Can you discover and prove a similar theorem about $\bigcap(\mathcal{F}\cup \mathcal{G})$?

(a) Suppose $x \in \bigcup(\mathcal{F}\cup \mathcal{G})$. Then we can choose some $A\in \mathcal{F}\cup \mathcal{G}$ such that $x \in A$. Since $A\in \mathcal{F}\cup \mathcal{G}$, either $A\in \mathcal{F}$ or $A\in \mathcal{G}$.

Case 1. $A\in \mathcal{F}$. Since $x \in A$ and $A\in \mathcal{F}$, $x \in \bigcup \mathcal{F}$, so $x \in\left( \bigcup \mathcal{F} \right)\cup\left( \bigcup \mathcal{G} \right)$.

Case 2. $A\in \mathcal{G}$. Since $x \in A$ and $A\in \mathcal{G}$, $x \in \bigcup \mathcal{G}$, so $x \in\left( \bigcup \mathcal{F} \right)\cup\left( \bigcup \mathcal{G} \right)$.

Thus, $x \in\left( \bigcup \mathcal{F} \right)\cup\left( \bigcup \mathcal{G} \right)$.

Now suppose that $x \in\left( \bigcup \mathcal{F} \right)\cup\left( \bigcup \mathcal{G} \right)$. Then either $x \in \bigcup \mathcal{F}$ or $x \in \bigcup \mathcal{G}$.

Case 1. $x \in \bigcup \mathcal{F}$. Then we can choose some $A\in \mathcal{F}$ such that $x \in A$. Since $A\in \mathcal{F}$, $A\in \mathcal{F}\cup \mathcal{G}$, so since $x \in A$, it follows that $x \in \bigcup(\mathcal{F}\cup \mathcal{G})$.

Case 2. $x \in \bigcup \mathcal{G}$. A similar arguement shows that $x \in \bigcup(\mathcal{F}\cup \mathcal{G})$. Thus, $x \in \bigcup(\mathcal{F}\cup \mathcal{G})$.

(b) 

The theorem is $\bigcap(\mathcal{F}\cup \mathcal{G})=\left( \bigcap \mathcal{F} \right)\cap \left( \bigcap \mathcal{G} \right)$.

($\to$) 

Suppose $x \in \bigcap(\mathcal{F}\cup \mathcal{G})$. Then Let $A$ be an arbitrary element of $\mathcal{F}$ and  $A\in \mathcal{F}$. $A\in \mathcal{F}\cup \mathcal{G}$. Since $x \in \bigcap(\mathcal{F}\cup \mathcal{G})$, $x \in A$. Since $A\in \mathcal{F}$ and $x \in A$, $x \in \bigcap \mathcal{F}$. A similar argument shows that $x \in \bigcap \mathcal{G}$. $x \in\left( \bigcap \mathcal{F} \right)\cap\left( \bigcap \mathcal{G} \right)$.

($\leftarrow$) 

Suppose $x \in\left( \bigcap \mathcal{F} \right)\cap\left( \bigcap \mathcal{G} \right)$. $x \in \bigcap \mathcal{F}$ and $x \in \bigcap \mathcal{G}$. Let $A$ be an arbitrary element of $\mathcal{F}\cup \mathcal{G}$. $A\in \mathcal{F}\cup \mathcal{G}$. Either $A\in \mathcal{F}$ or $A\in \mathcal{G}$.

Case 1. $A\in \mathcal{F}$. Since $x \in \bigcap \mathcal{F}$, $x \in A$. Thus, $x \in \bigcap(\mathcal{F}\cup \mathcal{G})$.

Case 2. $A\in \mathcal{G}$. A similar argument show that $x \in \bigcap(\mathcal{F}\cup \mathcal{G})$.

24.Suppose $A$, $B$, and $C$ are sets.
(a) Prove that $A\setminus C\subseteq(A\setminus B)\cup(B\setminus C)$.
(b) Prove that $A\triangle C\subseteq(A\triangle B)\cup(B\triangle C)$.

(a) Suppose $x \in A\setminus C$. $x \in A$ and $x\not\in C$.

Case 1. $x \in B$. $x \in B\setminus C$. Thus, $x \in (A\setminus B)\cup(B\setminus C)$.

Case 2. $x\not\in B$. $x \in A\setminus B$. Thus, $x \in(A\setminus B)\cup(B\setminus C)$.

(b)  
Suppose $x \in A\triangle C$. Either $x \in A\setminus C$ or $x \in C\setminus A$.

Case 1. $x \in A\setminus C$. 
	Case 1a. $x \in B$. $x \in B\setminus C$. Then $x \in B\triangle C$. $x \in(A\triangle B)\cup(B\triangle C)$.
	Case 2b. $x\not\in B$. $x \in A\setminus B$. Then $x \in A\triangle B$. $x \in(A\triangle B)\cup(B\triangle C)$.

Case 2. $x \in C\setminus A$.
	The proof is similar.

24.Suppose $A$, $B$, and $C$ are sets.

(a) Prove that $(A\cup B)\triangle C\subseteq(A\triangle C)\cup(B \triangle C)$.
(b) Find an example of sets $A$, $B$, and $C$ such that $(A\cup B)\triangle C\neq(A\triangle C)\cup(B\triangle C)$.

(a) 
Suppose $x \in(A\cup B)\triangle C$. Then either $x \in(A\cup B)\setminus C$ or $x \in C\setminus(A\cup B)$.

Case 1. $x \in(A\cup B)\setminus C$. Then either $x \in A$ or $x \in B$, and $x\not\in C$. We now break case 1 into two subcases, depending on whether $x \in A$ or $x \in B$.

Case 1a. $x \in A$. Then $x \in A\setminus C$, so $x \in A\triangle C$, so $x \in(A\triangle C)\cup(B\triangle C)$.

Case 1b. $x \in B$. Similarly, $x \in B\triangle C$, so $x \in(A\triangle C)\cup(B\triangle C)$.

Case 2. $x \in C\setminus(A\cup B)$. Then $x \in C$, $x\not\in A$, and $x\not\in B$. It follows that $x \in A\triangle C$ and $x \in B\triangle C$, so certainly $x \in(A\triangle C)\cup(B\triangle C)$.

(b) One example: $A=\{ 1 \}$, $B=\{ 2 \}$, $C=\{ 1,2 \}$.

27.Consider the following putative theorem.

Theorem? For every real number $x$, if $\lvert x-3 \rvert<3$ then $0<x<6$.

Is the following proof correct? If so, what proof strategies does it use? If not, can it be fixed? Is the theorem correct?

Proof. Let $x$ be an arbitrary real number, and suppose $\lvert x-3 \rvert< 3$. We consider two cases:

Case 1. $x-3\geq 0$. Then $\lvert x-3 \rvert=x-3$. Plugging this into the assumption $x-3<3$. $x <6$.

Case 2. $x-3< 0$. Then $x>0$.

Since we have proven both $0<x$ and $x<6$, we can conclude that $0<x<6$.

The theorem is correct. The proof is wrong.

Proof. Let $x$ be an arbitrary real number, and suppose $\lvert x-3 \rvert< 3$. We consider two cases:

Case 1. $x-3\geq 0$. Then $x\geq 3$. $x-3<3$. Thus, $x<6$. Since $3\leq x<6$, $0<x<6$.

Case 2. $x-3<0$. Then $x<3$. $\lvert x-3 \rvert=3-x<3$. $x>0$. $0<x<3$. Thus, $0<x<6$.

29.Consider the following putative theorem.

Theorem? $\forall x \in \mathbb{R}\exists y\in \mathbb{R}(xy^2\neq y-x)$.

Is the following proof correct? If so, what proof strategies does it use? If not, can it be fixed? Is the theorem correct?

Proof. Let $x$ be an arbitrary real number.

Case 1. $x=0$. Let $y=1$. Then $xy^2=0$ and $y-x=1-0=1$, so $xy^2\neq y-x$.

Case 2. $x\neq 0$. Let $y=0$. Then $xy^2=0$ and $y-x=-x\neq 0$, so $xy^2\neq y-x$.

Since these cases are exhaustive, we have shown that $\exists y\in \mathbb{R}(xy^2\neq y-x)$. Since $x$ was arbitrary, this shows that $\forall x \in \mathbb{R}\exists y\in \mathbb{R}(xy^2\neq y-x)$.

The proof is correct.

31.Consider the following putative theorem.

Theorem? Suppose $A$, $B$, and $C$ are sets and $A\subseteq B\cup C$. Then either $A\subseteq B$ or $A\subseteq C$.

Is the following proof correct? If so, what proof strategies does it use? If not, can it be fixed? Is the theorem correct?

Proof. Let $x$ be an arbitrary element of $A$. Since $A\subseteq B\cup C$, it follows that either $x \in B$ or $x \in C$.

Case 1. $x \in B$. Since $x$ was an arbitrary element of $A$, it follows that $\forall x \in A(x \in B)$, which means that $A\subseteq B$.

Case 2. $x \in C$. Similarly, since $x$ was an arbitrary element of $A$, we can conclude that $A\subseteq C$.

Thus, either $A\subseteq B$ or $A\subseteq C$.

The proof is wrong.